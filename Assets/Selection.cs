using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Selection : MonoBehaviour
{
//    [SerializeField] GameObject upgradeButtonPrefab;

//     [SerializeField]  Canvas canvas;
//     [SerializeField] LayerMask _plantCheck;

//     private void Update() 
//     {      


//          if (Input.GetMouseButtonDown(0))
//         {
//             Vector3 mousePosition = Camera.main.ScreenToWorldPoint(Input.mousePosition);
//             mousePosition.z = 0f;
//             RaycastHit2D hit = Physics2D.Raycast(mousePosition, Vector2.zero);
//             if (hit.collider != null && hit.collider.CompareTag("Selectable"))
//             {
                
//             //     GameObject upgradeButton = Instantiate(upgradeButtonPrefab, canvas.transform);
//             //     upgradeButton.transform.position = Camera.main.WorldToScreenPoint(hit.collider.transform.position);

//             //     upgradeButton.GetComponent<Button_Upgrading>().Upgrading(hit.collider.gameObject.name, hit.collider.gameObject);
//             //      Vector3 worldPosition = hit.transform.position;
//             //     Vector2 screenPosition = Camera.main.WorldToScreenPoint(worldPosition);

//             //     Vector2 canvasPosition;
//             //     RectTransformUtility.ScreenPointToLocalPointInRectangle(canvas.transform as RectTransform, screenPosition, canvas.worldCamera, out canvasPosition);

//             // upgradeButton.GetComponent<RectTransform>().anchoredPosition = canvasPosition;
//             }

//         }

//     }
   
}
